name: Cleanup PR Resources

on:
  pull_request:
    types: [ closed ]

jobs:
  cleanup:
    runs-on: github-ubuntu-latest-s
    permissions:
      actions: write
    steps:
      - uses: SonarSource/ci-github-actions/pr_cleanup@3c6565f5a65282d333ab60c8a0fa733c09e613d3 # 1.8.7
